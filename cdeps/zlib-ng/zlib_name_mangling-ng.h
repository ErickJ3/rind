/* zlib_name_mangling-ng.h — empty stub.
 *
 * Upstream's CMake fills this from zlib_name_mangling-ng.h.in when a
 * ZLIB_SYMBOL_PREFIX is set. We don't use one (rind links zlib-ng
 * statically into a single binary, no symbol collision risk), so the
 * file is a no-op header guard. zconf-ng.h includes this
 * unconditionally, so it has to exist.
 */

#ifndef ZLIB_NAME_MANGLING_H
#define ZLIB_NAME_MANGLING_H

#endif /* ZLIB_NAME_MANGLING_H */
